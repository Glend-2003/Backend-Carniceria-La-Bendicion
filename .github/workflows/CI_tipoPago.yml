name: CI/CD Pipeline - Carnicería La Bendición

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: bdcarniceria
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
    
    - name: Cache Maven dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
    
    - name: Wait for MySQL
      run: |
        until docker exec ${{ job.services.mysql.id }} mysqladmin ping --silent; do
          echo 'Waiting for MySQL...'
          sleep 2
        done
    
    - name: Run Unit Tests
      run: |
        echo "🧪 Ejecutando pruebas unitarias..."
        mvn test -Dtest=TipoPagoServiceTest
        
    - name: Run Integration Tests
      run: |
        echo "🔗 Ejecutando pruebas de integración..."
        mvn test -Dtest=TipoPagoControllerTest
        
    - name: Generate Test Report
      run: |
        echo "📊 Generando reporte de pruebas..."
        mvn surefire-report:report
        
    - name: Build Application
      run: |
        echo "🏗️ Construyendo aplicación..."
        mvn clean package -DskipTests
        
    - name: Send Email Notification on Success
      if: success()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: ✅ Pipeline Exitoso - Carnicería La Bendición
        body: |
          ¡Excelente trabajo! 🎉
          
          Las pruebas automatizadas han pasado exitosamente:
          - ✅ Pruebas unitarias del servicio TipoPago
          - ✅ Pruebas de integración del controlador
          - ✅ Construcción de la aplicación
          
          Commit: ${{ github.sha }}
          Rama: ${{ github.ref }}
          
          El código está listo para fusionar a MAIN.
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Carnicería CI/CD
        
    - name: Send Email Notification on Failure
      if: failure()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: ❌ Pipeline Fallido - Carnicería La Bendición
        body: |
          ¡Atención! ❌
          
          El pipeline ha fallado. Revisa los siguientes puntos:
          - Errores en las pruebas unitarias
          - Problemas en las pruebas de integración
          - Fallos en la construcción
          
          Commit: ${{ github.sha }}
          Rama: ${{ github.ref }}
          
          Por favor, revisa los logs para más detalles.
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Carnicería CI/CD

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: success()
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Merge to Main
      run: |
        git config --global user.name 'GitHub Actions'
        git config --global user.email 'actions@github.com'
        git fetch origin main
        git checkout main
        git merge origin/dev-qa --no-edit
        git push origin main
        
    - name: Deploy to Production
      run: |
        echo "🚀 Desplegando a producción..."
        # Aquí agregarías los comandos específicos de despliegue
        # Por ejemplo: scp, rsync, o comandos FTP
        echo "Aplicación desplegada exitosamente"
        
    - name: Send Deployment Notification
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: 🚀 Despliegue Exitoso - Carnicería La Bendición
        body: |
          ¡Despliegue completado! 🚀
          
          La aplicación ha sido desplegada exitosamente a producción:
          - ✅ Fusión automática DEV-QA → MAIN
          - ✅ Despliegue a servidor de producción
          
          Commit: ${{ github.sha }}
          
          La nueva versión está disponible.
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Carnicería CI/CD

